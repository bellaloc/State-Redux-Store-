{
  "name": "mern-shopping",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "start-client": "cd client && npm run dev",
    "install": "echo 'Installing dependencies' && npm install && echo 'Finished installing'",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm run build",
    "dev": "concurrently \"npm run seed\" \"npm run watch\" \"npm run start-client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.1.0"
  },
  "engines": {
    "node": "14.21.3",
    "npm": "6.14.18"
  }
}
